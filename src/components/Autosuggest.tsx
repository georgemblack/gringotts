function Autosuggest({
  value,
  suggestions,
  onChange,
}: {
  value: string;
  suggestions: string[];
  onChange: (value: string) => void;
}) {
  return <h1>hey</h1>;
}

export default Autosuggest;
