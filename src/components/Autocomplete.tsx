function Autocomplete({
  options,
  onSelect,
}: {
  options: string[];
  onSelect: (value: string) => void;
}) {
  return (
    <>
      <input></input>
    </>
  );
}

export default Autocomplete;
